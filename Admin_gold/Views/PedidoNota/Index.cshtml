@model IEnumerable<DataAdminGold.Ecom_PedidoNota>


<div class="table-responsive">
    <table class="table table-sm table-bordered table-striped">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Folio)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Pedido)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Folio)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Pedido)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit","PedidoNota", new { id = item.Id }) |
                        @Html.ActionLink("Details", "Details","PedidoNota", new { id = item.Id }) |
                        @Html.ActionLink("Delete", "Delete","PedidoNota", new { id = item.Id })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
